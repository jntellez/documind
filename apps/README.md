## apps/

Workspace directory reserved for application packages.

Current packages:

- `apps/mobile`: current Expo / React Native app
- `apps/api`: Bun + Hono backend service managed through the pnpm workspace
- `apps/web`: Next.js landing workspace for the official Android download flow and support/legal pages
